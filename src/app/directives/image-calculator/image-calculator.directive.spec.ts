import { ImageCalculatorDirective } from './image-calculator.directive';

describe('ImageCalculatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageCalculatorDirective();
    expect(directive).toBeTruthy();
  });
});
